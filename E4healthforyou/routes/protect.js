// const express = require('express');
// const router = express.Router();

// // Protected route
// router.get('/protected-route', (req, res) => {
//   // Perform authentication or authorization logic using the login cookie value
//   // If the user is authenticated, allow access to the protected content
//   // Otherwise, redirect to the login page or return an error response

//   res.status(200).json({ message: 'Protected route accessed' });
// });

// module.exports = router;
